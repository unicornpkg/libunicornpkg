# unix-path-bootstrap

This extra component makes the recommended POSIX-like paths work better with libunicornpkg.

This adds `/bin` to [the shell's path](https://tweaked.cc/module/shell.html#v:path) and `/usr/share/help` to [the `help` command's path](https://tweaked.cc/module/help.html#v:path).
