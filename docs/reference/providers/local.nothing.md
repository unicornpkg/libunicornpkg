# `local.nothing`

`local.nothing` is the identifier for a special package provider that does nothing.

It does not read or write files from the filesystem.
It does not read from `instdat` or any other table.
