# `local.generic`

`local.generic` is the identifier for a package provider that fetches from arbitrary URLs.

## `instdat.filemaps`

A table which contains mappings for files on GitLab and their local counterparts.

The key for each item in the table should correspond to the full URL (including `http://` or `https://`), and the value should be the local destination.
